define(function(){
    /*
    *接口映射表
    *
    key: {
        devUrl: //开发环境地址
        url: //线上环境地质
        methods: //兼容的HTTP方法
        require: //必填字段
        desc: //描述
    }
    **/
    var hostname = window.location.hostname;
    var APIBucket = {
        
    };

    return APIBucket;
});
